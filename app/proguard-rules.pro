# Add project specific ProGuard rules here.
# minifyEnabled is currently false (see app/build.gradle.kts), so these rules
# are inert until release minification is turned on. Left here so that switch
# is a one-line change later rather than a from-scratch proguard setup.
